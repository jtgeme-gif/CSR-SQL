-- Short reference notes on events (e.g. "contemplated motions in limine"),
-- not a full narrative field. Displayed inline, truncated to a "view record"
-- link past 100 characters.

ALTER TABLE events ADD COLUMN notes text;
